-m flag should just do everything in-terminal

-f works on files
overwrite by default
encrypt takes from raw, writes to encrypted
decrypt takes from encrypted, writes to decrypted

pass params to file_write instead of individual values